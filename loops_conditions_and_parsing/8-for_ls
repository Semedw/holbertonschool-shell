#!/usr/bin/env bash
#blablabla


for file in *; do
    # Skip directories
    if [ -f "$file" ]; then
        # Extract text after first dash
        after="${file#*-}"

        # If no dash exists, after == file → print filename
        # If dash exists, print only what's after the first dash
        echo "$after"
    fi
done
